# Configuration

Settings are loaded from `default.json`. Create `local.json` (gitignored) to override any values for your setup.

API keys are read from environment variables:
- `GROQ_API_KEY`
- `ANTHROPIC_API_KEY`
